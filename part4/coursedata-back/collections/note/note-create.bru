meta {
  name: note-create
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/notes
  body: json
  auth: none
}

body:json {
  {
    "content": "new note",
    "important": true
  }
}

script:pre-request {
  const { average } = require("./lib/test");
  
  console.log(average([1,2,3]));
}
